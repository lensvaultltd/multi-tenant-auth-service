version: '3.8'

services:
  auth-core:
    build: ./auth-core
    ports:
      - "8080:8080"

  admin-panel:
    build: ./admin-panel
    ports:
      - "3000:3000"

  client-app:
    build: ./client-app
    ports:
      - "3001:3000"
